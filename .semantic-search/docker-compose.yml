services:
  qdrant:
    image: qdrant/qdrant:v1.17.0
    container_name: qdrant-${PROJECT_NAME:-local}
    ports:
      - "6333:6333"   # REST API
      - "6334:6334"   # gRPC
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_storage:
