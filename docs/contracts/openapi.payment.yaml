openapi: 3.0.3
info:
  title: Payment API
  version: 1.0.0
paths:
  /payments:
    post:
      summary: Create payment
      responses:
        "201":
          description: Created
  /payments/{id}/cancel:
    post:
      summary: Cancel payment
      responses:
        "200":
          description: Cancelled