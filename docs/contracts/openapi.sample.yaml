openapi: 3.0.3
info:
  title: Sample API
  version: 1.0.0
paths:
  /samples:
    post:
      summary: Create sample
      responses:
        "201":
          description: Created
  /samples/{id}/cancel:
    post:
      summary: Cancel sample
      responses:
        "200":
          description: Cancelled